I am using tobac to do split and merge tracking of clouds.
First, what are: segment_labels, segments = tobac.segmentation_2D(....

Secondly, my code is: 
	trajectories = tobac.linking_trackpy(features, test_data_norm, dt=dt, dxy=dxy, v_max=v_max, memory=gap_features_frames)

cells_frames_before=[set() for _ in range(gap_features_frames)]
cell_ids=set()

for i, itime in enumerate(range(0, frames_no)):
    # plot the 2D blob field in colors
    
    # %% segmentation
            
    smoothed_frame = ndimage.gaussian_filter(test_data_norm.isel(time=itime).values, sigma=3)
    temp_da = test_data_norm.isel(time=itime).copy()
    temp_da.data = smoothed_frame
    temp_da.plot(ax=axs[i], vmin=0, vmax=1, cmap="viridis")


    temp_da = test_data_norm.isel(time=itime).copy()
    temp_da.data = smoothed_frame

    temp_da = test_data_norm.isel(time=[itime]).copy()
    temp_da.data = smoothed_frame[np.newaxis, ...]  # keep time dim

    field_2d = temp_da


    f = features[features["frame"] == itime] 

    segment_labels, segments = tobac.segmentation_2D(
            f,
            field_2d,
            dxy=dxy,
            threshold=threshold,
            target="minimum"
        )


 secondly, make so that for each feature, we add a "border of "x px" around it in the picture so i can see whats going on.

